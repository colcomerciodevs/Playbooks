# Directorio de salida (relativo al playbook)
out_dir: "{{ playbook_dir }}/salidas_docker"

# Nombre del archivo Excel
excel_name: "docker_stats.xlsx"

# Umbrales de riesgo
cpu_risk_high: 85
cpu_risk_medium: 70
mem_risk_high: 90
mem_risk_medium: 75
pids_risk_high: 400
pids_risk_medium: 200
