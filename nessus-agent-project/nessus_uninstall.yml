- name: Remover Nessus
  hosts: OCI_PEOPLE
  gather_facts: true
  become: true

  tasks:
    - name: Remover Nessus
      ansible.builtin.import_role:
        name: nessus_agent
        tasks_from: uninstall.yml
