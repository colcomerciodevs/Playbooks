- name: Remover Nessus
  hosts: ansible_test
  gather_facts: true
  become: true

  tasks:
    - name: Remover Nessus
      ansible.builtin.import_role:
        name: nessus_agent
        tasks_from: uninstall.yml
