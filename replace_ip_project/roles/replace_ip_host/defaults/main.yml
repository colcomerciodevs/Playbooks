---
# Ruta raíz a escanear recursivamente
scan_root: "/home/psoft85/prueba"

# IP literal a reemplazar (exacta)
old_ip: "10.181.0.77"

# Hostname destino (ej. lo que tengas en /etc/hosts o DNS)
new_host: "batch01"

# Patrones de archivos a evaluar
file_patterns:
  - "*.sh"
  - "*.bash"

# Si lo dejas vacío, el rol construye un regex exacto con bordes \\b
# para evitar reemplazos parciales.
old_ip_regex: ""

# Si true, captura evidencia de líneas que contengan el hostname tras el cambio
capture_after_host_lines: true
